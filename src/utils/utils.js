/**
 *
 * Library of reusable functions
 * --------------------------------
 *
 * Useable by importing them from _utils
 *
 */

// --- Functions ---
export { diff, clamp, map, timestamp } from './functions/math'

export { capitalize, randomString, slugify, readBytes } from './functions/helpers'

export { useAnimationFrame } from './useAnimationFrame/useAnimationFrame'

export { isDateBeforeToday, renderImg, postType, postSlug } from './functions/functions'
